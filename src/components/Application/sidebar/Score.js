import React from 'react';
import styled from 'styled-components';

const ScoreWrapper = styled.div`
  flex-grow: 1;
`;

const Score = () => <ScoreWrapper>Score</ScoreWrapper>;

export default Score;
